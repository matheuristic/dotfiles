[diff]
	# tool = meld
	tool = xxdiff
[difftool]
	prompt = false
[merge]
	# tool = meld
	tool = xxdiff
[mergetool]
	prompt = false
[alias]
	diffdir = difftool --dir-diff

# macOS settings for [Meld for OSX](https://yousseb.github.io/meld/)

# [difftool "meld"]
# 	trustExitCode = true
# 	cmd = open -W -a Meld --args \"$LOCAL\" \"$PWD/$REMOTE\"
# [mergetool "meld"]
# 	trustExitCode = true
# 	cmd = open -W -a Meld --args --auto-merge \"$PWD/$LOCAL\" \"$PWD/$BASE\" \"$PWD/$REMOTE\" --output=\"$PWD/$MERGED\"
